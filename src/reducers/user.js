let initUser= {};
let userReducers = (state=initUser,action)=>{
    switch (action.type) {
        
    
        default:
            return state ;
            break;
    }
}
export default userReducers;